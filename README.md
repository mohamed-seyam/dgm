# Deep Generative Models

From-scratch implementations of **VAE** and **DDPM**, trained and evaluated on MNIST.

---

## Setup

```bash
conda create -n dgm python=3.10 -y
conda activate dgm
pip install -r requirements.txt
```


